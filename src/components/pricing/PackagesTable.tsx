"use client"

interface Package {
  id: string
  name: string
  items: string[]
  totalPrice: number
  discountedPrice: number
  isActive: boolean
}

export function PackagesTable() {
  return (
    <div>
      {/* Implementación pendiente */}
    </div>
  )
}
